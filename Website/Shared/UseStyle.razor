@inject IJSRuntime JSRuntime;

@code {
    [Parameter]
    public string url { get; set; }

    protected async Task OnAfterRenderAsync()
    {
        await JSRuntime.InvokeVoidAsync("LoadStyle", url);
    }
}
